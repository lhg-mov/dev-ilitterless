import React from "react";

const IntroSlideClient = ({ children }: { children: React.ReactNode }) => {
  return <div className="intro__section__two">{children}</div>;
};

export default IntroSlideClient;
