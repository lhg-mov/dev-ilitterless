import React from "react";

const IntroCampaignClient = ({ children }: { children: React.ReactNode }) => {
  return <div className="intro_section__three">{children}</div>;
};

export default IntroCampaignClient;
