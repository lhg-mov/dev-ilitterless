import React from "react";

const IntroTitleClient = ({ children }: { children: React.ReactNode }) => {
  return <div className="intro__section__one">{children}</div>;
};

export default IntroTitleClient;
